package main

import (
	"fmt"
)

func main() {
	var sms int
	var consti float64
	var per bool
	var username string

	fmt.Printf(
		"%v %f %v %q\n",
		sms,
		consti,
		per,
		username,
	)

	if length := getLength(email); length < 1 {
		fmt.Println("Hye")
	}

	// var empty string or empty:= ""
}